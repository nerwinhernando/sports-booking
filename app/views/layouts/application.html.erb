<!DOCTYPE html>
<html>
  <head>
    <title><%= current_account&.name || 'Badminton Court Booking' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100">
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3" role="alert">
        <p class="text-center"><%= notice %></p>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3" role="alert">
        <p class="text-center"><%= alert %></p>
      </div>
    <% end %>

    <%= yield %>

  <!-- Footer -->
  <footer class="bg-white border-t border-gray-200 mt-12 py-6">
    <div class="container mx-auto px-4 text-center text-gray-600 text-sm">
      <p>© <%= Time.current.year %> BadmintonPH Admin Panel</p>
    </div>
  </footer>
  </body>
</html>